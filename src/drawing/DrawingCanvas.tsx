import React,{useRef,useState} from 'react';
import './drawing.css';
type Stroke={points:{x:number;y:number}[];size:number};
export default function DrawingCanvas(){const ref=useRef<HTMLCanvasElement>(null);const [drawing,setDrawing]=useState(false);const [size,setSize]=useState(4);const start=(e:React.PointerEvent)=>{const c=ref.current;if(!c)return;const r=c.getBoundingClientRect();const ctx=c.getContext('2d')!;ctx.lineCap='round';ctx.lineJoin='round';ctx.lineWidth=size;ctx.beginPath();ctx.moveTo((e.clientX-r.left)*c.width/r.width,(e.clientY-r.top)*c.height/r.height);c.setPointerCapture(e.pointerId);setDrawing(true)};const move=(e:React.PointerEvent)=>{if(!drawing)return;const c=ref.current;if(!c)return;const r=c.getBoundingClientRect();const ctx=c.getContext('2d')!;ctx.lineTo((e.clientX-r.left)*c.width/r.width,(e.clientY-r.top)*c.height/r.height);ctx.stroke()};return <div className="drawing-tool"><div className="drawing-controls"><b>VECTOR / DRAW</b><label>Brush <input type="range" min="1" max="40" value={size} onChange={e=>setSize(+e.target.value)}/></label><button onClick={()=>{const c=ref.current;c?.getContext('2d')?.clearRect(0,0,c.width,c.height)}}>Clear</button></div><canvas ref={ref} width={1920} height={1080} onPointerDown={start} onPointerMove={move} onPointerUp={()=>setDrawing(false)} /></div>}
