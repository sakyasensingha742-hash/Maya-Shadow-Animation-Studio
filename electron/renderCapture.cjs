const fs=require('fs');const path=require('path');const {BrowserWindow}=require('electron');
async function requestFrame(webContents,frame,requestId){const result=await webContents.executeJavaScript(`(async()=>{const payload=${JSON.stringify({frame,requestId})};window.__mayaShadowCaptureRequest=payload;if(typeof window.__mayaShadowRequestFrame==='function'){window.__mayaShadowRequestFrame(payload)}else{const ranges=[...document.querySelectorAll('input[type="range"]')].filter(el=>{const min=Number(el.min||0),max=Number(el.max||0);return max>=payload.frame&&min<=payload.frame});const target=ranges.find(el=>/frame|timeline/i.test([el.getAttribute('aria-label'),el.title,el.name].filter(Boolean).join(' ')))||ranges.find(el=>Number(el.max||0)>=payload.frame&&Number(el.max||0)<=1000);if(target){const setter=Object.getOwnPropertyDescriptor(HTMLInputElement.prototype,'value')?.set;if(setter)setter.call(target,String(payload.frame));else target.value=String(payload.frame);target.dispatchEvent(new Event('input',{bubbles:true}));target.dispatchEvent(new Event('change',{bubbles:true}))}}await new Promise(r=>requestAnimationFrame(()=>requestAnimationFrame(r)));window.__mayaShadowCaptureReady=payload.requestId;return true})()`);return result}
function waitForRendererReady(webContents,requestId,timeoutMs=10000){return new Promise((resolve,reject)=>{const started=Date.now();const timer=setInterval(()=>{if(Date.now()-started>timeoutMs){clearInterval(timer);reject(new Error('Timed out waiting for animation frame to render.'));return}webContents.executeJavaScript(`window.__mayaShadowCaptureReady===${JSON.stringify(requestId)}||Boolean(window.__mayaShadowRenderReady&&window.__mayaShadowRenderReady(${JSON.stringify(requestId)}))`).then(ok=>{if(ok){clearInterval(timer);resolve()}}).catch(()=>{})},25)})}
async function prepareRenderViewport(webContents,width,height){const win=BrowserWindow.fromWebContents(webContents);if(!win)throw new Error('Render window is unavailable.');const state={bounds:win.getBounds(),visible:win.isVisible(),maximized:win.isMaximized(),fullscreen:win.isFullScreen()};if(state.fullscreen)win.setFullScreen(false);if(state.maximized)win.unmaximize();win.hide();win.setContentSize(Math.max(320,Math.round(width)),Math.max(240,Math.round(height)),false);await webContents.executeJavaScript(`(()=>{let s=document.getElementById('__mayaShadowRenderStyle');if(!s){s=document.createElement('style');s.id='__mayaShadowRenderStyle';document.head.appendChild(s)}s.textContent=${JSON.stringify(`html,body,#root{width:${width}px!important;height:${height}px!important;overflow:hidden!important}.studio{display:block!important;width:${width}px!important;height:${height}px!important}.studio>.topbar,.studio>.toolbar,.studio>.timeline,.studio>footer{display:none!important}.workspace{display:block!important;width:${width}px!important;height:${height}px!important}.left-panel,.right-panel,.canvas-tabs{display:none!important}.canvas-area{display:block!important;width:${width}px!important;height:${height}px!important;overflow:hidden!important}.stage-wrap{display:block!important;width:${width}px!important;height:${height}px!important;overflow:hidden!important;background:#000!important}.stage{width:${width}px!important;height:${height}px!important;max-width:none!important;aspect-ratio:auto!important;transform:none!important;box-shadow:none!important}`);return true})()`);await new Promise(r=>setTimeout(r,50));return async()=>{await webContents.executeJavaScript(`document.getElementById('__mayaShadowRenderStyle')?.remove();true`).catch(()=>{});win.setBounds(state.bounds);if(state.fullscreen)win.setFullScreen(true);else if(state.maximized)win.maximize();if(state.visible)win.show();}}}
async function captureFrame(webContents,frame,requestId,outputPath,width,height){await requestFrame(webContents,frame,requestId);await waitForRendererReady(webContents,requestId);const image=await webContents.capturePage({x:0,y:0,width:Math.max(1,Math.round(width)),height:Math.max(1,Math.round(height))},{stayHidden:true});const targetWidth=Math.max(1,Number(width)||image.getSize().width);const targetHeight=Math.max(1,Number(height)||image.getSize().height);const size=image.getSize();const bitmap=size.width===targetWidth&&size.height===targetHeight?image:image.resize({width:targetWidth,height:targetHeight});fs.writeFileSync(outputPath,bitmap.toPNG());return{width:targetWidth,height:targetHeight}}
async function captureSequence(webContents,options,onProgress){const start=Math.max(1,Number(options.startFrame)||1),end=Math.max(start,Number(options.endFrame)||start),dir=options.framesDir;fs.mkdirSync(dir,{recursive:true});const total=end-start+1;const restoreViewport=await prepareRenderViewport(webContents,Number(options.width)||1920,Number(options.height)||1080);try{for(let frame=start;frame<=end;frame++){if(options.isCancelled?.())throw new Error('Render cancelled');const requestId=`capture-${Date.now()}-${frame}`;const outputPath=path.join(dir,`frame_${String(frame).padStart(6,'0')}.png`);await captureFrame(webContents,frame,requestId,outputPath,Number(options.width)||1920,Number(options.height)||1080);onProgress?.({status:'capturing',phase:'frames',frame,progress:Math.round(((frame-start+1)/total)*50)})}return{start,end,framesDir:dir}}finally{await restoreViewport()}}
module.exports={captureSequence};